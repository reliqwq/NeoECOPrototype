# NeoECOPrototype 开发日志

日期：2026-09-11  
版本状态：未发布、未提交，不修改已发布的 `NeoECOPrototype 1.0.4`

## 追加阶段：超越维度（Beyond Dimensions）存储矩阵

目标：让 Neo ECO 的存储矩阵直接引用 Beyond Dimensions 的跨维度网络存储。

### 已实现

新增 `item.neoecoprototype.beyond_storage_cell`（`beyond_storage_cell`）：

```text
ECO - 超越维度存储矩阵 / ECO - Beyond Dimensions Storage Matrix
```

实现方式是一条极薄的适配链，没有复制 Beyond 的存储逻辑：

```text
BeyondStorageCellItem（绑定网络的物品）
        ↓
BeyondStorageCellHandler（注册到 eco 的 ECOStorageCells）
        ↓
Beyond NetStorageCell（Beyond 自带的 AE2 适配）
        ↓
DimensionsNet.UnifiedStorage
```

因此物品的插入、提取、快照和增量更新全部沿用 Beyond 自己的实现：

- `NetStorageCell.insert/extract` 负责实际读写；
- `NetStorageCell.getAvailableStacks` 提供 AE2 侧快照；
- Beyond 的 `subscribeDeltaWeak` / `subscribeAnyWeak` 负责缓存增量刷新。

Neo ECO 侧只做元数据适配：

- `getTier()` → `SimplifyTier.L1`，因此只能放进 L1 驱动器和接受 L1 的宿主；
- `getKeyType()` → `AEKeyType.items()`，首版只支持物品；
- `getCellType()` → 本模组自己的 `ECOCellType`，用于宿主 UI 分类；
- 容量对外报告为外部网络引用型，不额外限制总量；
- `getConfigInventory`/`setFuzzyMode` 保持无效，不伪造分区，避免误报容量。

### 依赖与可选性

- Beyond Dimensions 通过 `ModList.get().isLoaded("beyonddimensions")` 判断；
- 未安装时既不注册物品，也不注册 Cell Handler；
- `build.gradle` 中该构件为 `compileOnly`，不打包、不强制依赖。

### 已知限制

- 仅物品，不含流体、能量和化学品；
- 不支持分区和模糊模式；
- 容量显示为外部引用，不会显示 Beyond 网络的真实占用比；
- 还未在装有 Beyond Dimensions 的实例中做功能验收。

## 本阶段完成内容

### F1 合成子系统

- 将 F1 默认多方块中的合成接口改为本模组的 `SimplifyCraftingInterfaceBlock`。
- F1 接口覆盖 `useWithoutItem(...)`，右键不再打开 GUI，但仍保留合成网络接口功能。
- 调整名称：
  - 中文：`F1 合成子系统接口`
  - 英文：`F1 Crafting Subsystem Interface`
- 默认多方块位置确认使用无 GUI 接口，没有改用 Network Interface。
- F1 初始并行参数保留为：
  - 普通并行：16
  - 超频并行：32
- F1 合成组件和控制器的待机耗电基准降低一半。
- 相关功耗单元测试已同步更新。

### L1 化学品存储矩阵

新增 Mekanism/AppMek 可选化学品存储内容：

- 化学品存储矩阵外壳；
- 1K 化学品存储矩阵；
- 16K 化学品存储矩阵；
- 1M 化学品存储矩阵；
- 4M 化学品存储矩阵。

外壳配方沿用 L1 物品存储矩阵外壳结构，将铝锭替换为：

```text
mekanism:basic_control_circuit
```

Cell 配方规则：

```text
化学品矩阵外壳 + 对应容量组件
→ 对应容量化学品存储矩阵
```

1K/16K 使用 AE2 的对应 Cell Component；1M/4M 使用本模组的：

```text
neoecoprototype:simplify_storage_component_1m
neoecoprototype:simplify_storage_component_4m
```

四个容量档位均已添加拆卸配方，可拆回化学品矩阵外壳和对应容量组件。

化学品 Cell 仅在 Mekanism 与 AppMek 同时加载时注册；本地测试依赖保持可选，不应成为发布版本的强制依赖。

## 主动冷却问题调查

本阶段重点调查了 F1 开启主动冷却后处理时间显示为 4 的问题。

Neo ECO 的处理时间由能力快照中的 `effectiveOverclock` 参与计算。工作线程调用链为：

```text
ECOCraftingWorkerBlockEntity.tickingRequest()
→ ECOCraftingSystemBlockEntity.getEffectiveOverclockTimes()
→ ECOCraftingThread.tick(...)
→ calculateProgressPerTick(...)
```

界面时间换算大致为：

```text
ceil(10 / (effectiveOverclock + 1))
```

因此：

```text
effectiveOverclock = 0 → 10 ticks
effectiveOverclock = 2 → 4 ticks
effectiveOverclock = 9 → 1 tick
```

调查过程中确认，主动冷却、超频开关、并行核心、工作核心、冷却剂上限和网络集群能力快照会共同参与计算。Neo ECO 当前存在冷却收益与设备等级没有正确绑定的问题：低等级设备可能享受高级冷却液收益，而无冷却液状态也可能得到不符合预期的速度档位。

## 已回退的实验改动

以下修改均已回退，没有作为最终方案保留：

- 修改冷却配方的 `maxOverclock`；
- 修改 `getLocalCoolingMaxOverclock()`；
- 修改 `getEffectiveOverclockTimes()`；
- 强制主动冷却返回超频值 9；
- 修改冷却剂消耗量；
- 修改 Neo ECO 的线程处理时间逻辑。

回退原因：这些方式会绕过 Neo ECO 原本由并行核心、工作核心、主动冷却和冷却剂等级共同决定的机制，可能造成：

- UI 显示能力与实际吞吐量不一致；
- 低等级设备获得不应有的高级冷却收益；
- 并行数量和处理时间互相脱节；
- 后续 Neo ECO 修复 Bug 后产生重复或冲突逻辑。

当前 F1 已恢复使用 Neo ECO 原版主动冷却/超频机制。等 Neo ECO 上游修复后，本模组应自然继承修复，不再需要 F1 专用冷却补丁。

## Mixin 审计与收窄

本阶段审计了所有 Mixin，确认当前没有残留主动冷却、超频、线程时间或冷却剂逻辑注入。

保留的 F1 相关注入：

- `ECOCraftingSystemBlockEntityMixin`
  - 只替换 L1 F1 的多方块定义；
  - 只为 L1 F1 控制器设置待机功耗。
- `NECraftingClusterCalculatorMixin`
  - 只在检测到本模组 L1 控制器时使用本模组结构验证器。
- `ECOCraftingHighPowerMixin`
  - 只对本模组注册的 F1 组件方块调整待机功耗。

本阶段对 `NEBlockEntityPowerMixin` 做了最小收窄：

- 增加 `require = 1`；
- 增加 Block Registry ID 空值防护；
- 继续严格限制为 `neoecoprototype` 命名空间；
- 功耗数值未改变。

该 Mixin 暂时不能删除，因为部分本模组通用结构部件仍依赖它设置基础待机功耗。后续可以逐个将功耗初始化迁移到本模组自己的 BlockEntity，再删除这个全局基类注入。

## 验证记录

本阶段多次通过：

```text
./gradlew.bat compileJava --offline
./gradlew.bat compileJava test --offline
./gradlew.bat processResources --offline
```

最近一次收窄 `NEBlockEntityPowerMixin` 后：

```text
compileJava BUILD SUCCESSFUL
test BUILD SUCCESSFUL
```

客户端启动验证通过，加载确认包括：

- NeoECOPrototype；
- Neo ECO AE Extension；
- Mekanism；
- Applied Mekanistics；
- AE2 JEI Integration；
- JEI；
- Jade；
- F1/C1/存储系统相关资源和类。

## 当前结论

当前代码策略：

```text
F1 结构、接口和功耗：使用本模组实现
化学品存储矩阵：使用本模组完整实现
主动冷却/超频：恢复 Neo ECO 原版机制
```

后续维护原则：

1. 不在本模组中重复修复 Neo ECO 的主动冷却 Bug；
2. 不直接改 `effectiveOverclock`、冷却配方或线程计时；
3. 需要继续收窄 Mixin 时，一次只处理一个注入点；
4. 每一步都执行编译、测试和客户端启动验证；
5. 不发布或重新发布 `NeoECOPrototype 1.0.4`。
