# NeoECOPrototype 开发日志

日期：2026-09-12

## 本阶段：无限混凝土存储矩阵

目标：新增一套不依赖超越维度（也不依赖任何模组，纯原版可用）的"无限混凝土"存储矩阵。

### 设计定位（参照扩展 AE 的 ME 无限元件）

第一版做成了"能装无限多混凝土"的收纳型格子，方向错误，已重做。最终版对齐
ExtendedAE"ME 无限水/圆石元件"的机制——**无限取出源**：

- 装入 L1 驱动器并成簇后，ME 网络中永远有 16 色混凝土可供无限提取；
- 任何物品都无法存入（`insert` 恒为 0），它不是收纳容器；
- 格子无状态，没有持久化数据。

### 新增内容

- `simplify_concrete_storage_matrix_housing`：ECO - L1 混凝土存储矩阵外壳；
- `simplify_concrete_storage_cell`：ECO - L1 无限混凝土存储矩阵。

### 实现方式

沿用超越维度矩阵的适配链模式，全部是元数据与行为适配，没有复制存储引擎：

```text
SimplifyConcreteStorageCellItem（纯元数据：L1 / items / 16 类 / 无限字节）
        ↓
InfiniteConcreteCellHandler（注册到 eco 的 ECOStorageCells，commonSetup 无条件注册）
        ↓
Cell（无状态 IECOStorageCell：extract 恒成功、insert 恒拒绝）
```

`Cell` 的行为细节（存取一体，方向对齐"无限仓库"）：

- `insert(what, amount)` → `what` 是 16 色混凝土之一即全额接收并融入
  无限库存（不入账、不追踪），普通矩阵不再囤积混凝土；
- `isPreferredStorageFor` → 混凝土返回 true，AE2 路由时优先选本矩阵，
  配合全额接收实现"混凝土只认无限矩阵"（若其他矩阵优先级更高仍会先入
  那边，用宿主 UI 的优先级调整即可）；
- `extract(what, amount)` → `what` 是 16 色混凝土之一即返回 `amount`，模拟与
  实际执行均成功、无状态变化；
- `getAvailableStacks` → 每色报告 `Long.MAX_VALUE`（终端里显示为天量库存）；
- `getStatus()` → 恒为 `NOT_EMPTY`；`canFitInsideCell()` → false（无限源不允许
  被收进另一个格子）；`persist()` → 空操作；
- `getCellType()` → 复用 eco 的物品 cell type（宿主 UI 归入物品行，驱动器按
  物品类渲染，与猪咪矩阵一致）；
- `getTier()` → `SimplifyTier.L1`；待机功耗固定 8.0 AE/t；
- `getUsedBytes()` → 0，`getTotalBytes()` → 16。显示数值按需求多轮调整后定稿：
  字节预算只报格子的自身 footprint（16 色每色 1 个类型槽 × 1 字节 = 16 字节，
  信息栏显示"0 / 16 字节"），供给量不用字节表达；
  终端里每种混凝土库存对齐 ExtendedAE 无限元件的做法报
  `Integer.MAX_VALUE`（约 2.1G，`REPORTED_AMOUNT_PER_TYPE`），
  提取本身依旧不受上限约束。

### 配方

- 外壳：绿晶晶格 + 沙 + 砾石（沿用 ABA/B B/CCC 外壳结构，呼应混凝土粉末的原料）；
- 合成：混凝土矩阵外壳 + `neoecoprototype:simplify_storage_component_4m`
  → 无限混凝土存储矩阵；
- 拆卸：`ae2:storage_cell_disassembly`，可拆回外壳与 L4 存储组件。

### 资源

- 物品贴图 `concrete_cell_housing.png` 由 `eco_item_cell_housing.png` 去饱和成
  混凝土灰程序化生成，属于占位美术，后续可在贴图工作区手工重绘；
- 中英语言条目与灰字描述（"可从网络中无限取出混凝土（16 色）"）已添加；
- 创造模式标签在外壳分组中紧邻化学品外壳。

### 验证记录

```text
./gradlew.bat compileJava test processResources --offline
BUILD SUCCESSFUL
```

gametest 的 `registrationsAreAvailable` 增加了混凝土矩阵注册断言（无条件，
因为它不依赖任何模组）。

`runGameTestServer` 实测：模组加载与注册全部正常（加载日志无报错、测试批次启动），
但测试本身卡住——仓库中从未提交过 gametest 所需的 `empty` 结构模板
（`1d8825d` 引入 gametest 时遗留），与本次改动无关。运行时验证以
客户端启动验证为准。

### 已知限制

- 只覆盖 16 色硬化混凝土，混凝土粉末不在供给范围内；
- 宿主 UI 混凝土信息栏字节显示为 0 / 16 字节（纯 footprint 标注）；
- 与扩展 AE 不同，本矩阵的无限提取不在 IO 端口内做能量换算，
  能耗遵循 L1 驱动器与矩阵自身的待机功耗体系。
